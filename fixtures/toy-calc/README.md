# Fixture: toy-calc

Live-e2e consumer fixture. Run the engine against it:

```sh
node ../../dist/cli.mjs run --plan plans/<plan>.md --cwd .
```

Run state lands in `.lightsout/runs/` (gitignored at repo root).
