---
description: 'Run the lightsout deterministic implementation pipeline on a plan file. Use when the user asks to implement a plan via lightsout.'
---
<!-- generated:lightsout-command -->

Load the lightsout `implement` skill and follow it exactly as if the user had invoked it directly — on the pi-family harnesses (omp, pi) read `skill://implement`; in Claude Code open the `implement` skill from your skills list.

User input: $ARGUMENTS
