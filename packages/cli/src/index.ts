/**
 * Entry point bundled to dist/cli.mjs. Commands (v0): run, resume, status.
 */
const main = () => {
	console.log('lightsout — deterministic engine for coding agents (pre-alpha)');
	console.log('usage: lightsout <run|resume|status> ...');
};

main();
