/** Why a finding is unjudged when no judge ever ruled on it and the caller gave no reason of its own — the judge fan-out stopped before its batch started, or no batch held it. */
export const noJudgeRanReason = 'no judge ran — the fan-out stopped before this finding was judged';
