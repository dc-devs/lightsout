import { GapCheckLens } from '#src/contracts/plan/grade/GapCheckLens.ts';

/** Every lens, in declaration order — the gap-check fan-out's width per plan file. */
export const gapCheckLenses = Object.values(GapCheckLens);
