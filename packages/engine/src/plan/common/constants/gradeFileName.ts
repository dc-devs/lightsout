/** The plan folder's latest grade verdict. Spelled once, because the runner that writes it and the durable-file list that publishes it must never disagree. */
export const gradeFileName = 'grade.json';
