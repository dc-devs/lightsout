// The two template-rule renderers and the four brief sections are this module's
// private companions: each is one slab of prompt prose that only this builder
// assembles, and each is covered through it. Published here is the one thing the
// draft loop asks for — the finished invocation.

export { buildPlanWriterInvocation } from '#src/agents/buildPlanWriterInvocation/buildPlanWriterInvocation.ts';
