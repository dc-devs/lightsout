/**
 * The brainstorm's write-up. Both attachment generations carry it: the
 * brainstorm publishes it as its own record, and the plan publishes it as the
 * shaping that preceded the plan. Neither owns the title, which is why every
 * "is there a plan/brainstorm on this ticket?" test excludes it.
 */
export const brainstormNotesFileName = 'brainstorm-notes.md';
