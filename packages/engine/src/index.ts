/**
 * The deterministic spine: pipeline steps, gates, budgets, run-manifest state,
 * and resume. No model calls happen here — the engine hands work to a driver
 * and validates what comes back. Gates are `if` statements; budgets are
 * counters; nothing in this package can be sweet-talked.
 */
export {};
