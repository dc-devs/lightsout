// The config locator is this module's private companion: it serves only the
// load below, and is covered through it. Published here is the one read every
// view over a scope's Jest configuration is built from.

export { loadScopeJestConfig } from '#src/coverage/loadScopeJestConfig/loadScopeJestConfig.ts';
