/**
 * The activity record's name inside whatever directory holds it.
 *
 * Stated here rather than only inside the path helper, so a reader can
 * recognise a record in a directory listing without building a path first.
 */
export const activityRecordFileName = 'activity.jsonl';
