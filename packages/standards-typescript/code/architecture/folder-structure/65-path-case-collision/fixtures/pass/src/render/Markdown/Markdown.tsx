// The resolution: the declaration file was renamed for what it declares, so
// the graduated PascalCase folder keeps its casing and nothing collides.
export const Markdown = () => <section>markdown</section>;
