// Kebab-case throughout: the folder should be camelCase and the document
// PascalCase, so nothing in the tree says which file is which kind.
export const CountIssuesDocument = 'query CountIssues { issues { totalCount } }';
