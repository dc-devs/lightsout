// `renderGreeting` is published by the package entry and `buildGreeting` used
// by it — every export in the package has something that consumes it.
export { renderGreeting } from './feature/renderGreeting';
