/**
 * The address word for the `base` channel. The rules every repo gets are the
 * TypeScript rules to a reader, so their page is `/standards-packs/typescript`;
 * every other channel is addressed by its own id.
 */
export const baseRuleSetSlug = 'typescript';
