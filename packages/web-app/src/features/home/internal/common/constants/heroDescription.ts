/**
 * The hero's first sub line, which is also the sentence a search result or a
 * shared post shows under the title.
 *
 * One copy for both: the visible paragraph and the `description` tag say the
 * same thing by construction, so an edit to the page cannot leave the shared
 * link quoting the sentence the page no longer carries.
 */
export const heroDescription =
	'Your coding agent solves the task in front of it and moves on. A second helper here, a second pattern there, the repo’s worst shortcut copied as precedent. Every change passes. The codebase rots anyway.';
