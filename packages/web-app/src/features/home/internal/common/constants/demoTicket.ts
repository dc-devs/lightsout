/**
 * The one ticket the home page follows: the run card's heading and the ticket
 * drawn in the tracker section both name it, so the two read as the same work.
 */
export const demoTicket = { key: 'LO-155', title: 'Implement module dependency graph', branch: 'LO-155-dependency-graph' } as const;
