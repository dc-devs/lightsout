{
	"name": "lightsout",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Deterministic engine for coding agents — the accountability shell that lets the factory run with the lights off.",
	"license": "MIT",
	"author": "David Christian",
	"engines": {
		"node": ">=22.18.0"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "pnpm test:unit && pnpm test:e2e",
		"test:unit": "jest -c tests/config/jest.config.cjs",
		"test:unit:coverage": "jest -c tests/config/jest.config.cjs --coverage --passWithNoTests",
		"test:e2e": "jest -c tests/config/jest.e2e.config.cjs",
		"bundle": "pnpm bundle:engine && pnpm bundle:standards",
		"bundle:engine": "esbuild src/cli/index.ts --bundle --platform=node --format=esm --loader:.md=text --banner:js=\"import { createRequire as __cjsRequire } from 'node:module'; const require = __cjsRequire(import.meta.url);\" --outfile=plugin/dist/cli.mjs",
		"bundle:standards": "node scripts/copyStandards.mjs",
		"lightsout": "node  plugin/dist/cli.mjs"
	},
	"dependencies": {
		"yaml": "^2.9.0",
		"zod": "^4.0.0"
	},
	"devDependencies": {
		"@jest/globals": "^30.4.1",
		"@jscpd/core": "^4.2.5",
		"@jscpd/tokenizer": "^4.2.5",
		"@types/node": "^22.10.0",
		"esbuild": "^0.24.0",
		"jest": "^30.4.2",
		"ts-jest": "^29.4.12",
		"typescript": "^5.7.0"
	}
}
