{
	"name": "lightsout",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Deterministic engine for coding agents — the accountability shell that lets the factory run with the lights off.",
	"license": "MIT",
	"author": "David Christian",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"check": "tsc --noEmit",
		"test": "rm -rf .test-dist && esbuild $(find packages -name '*.test.ts' -not -path '*/node_modules/*') --bundle --platform=node --format=esm --loader:.md=text --banner:js=\"import { createRequire as __cjsRequire } from 'node:module'; const require = __cjsRequire(import.meta.url);\" --outbase=. --outdir=.test-dist --log-level=warning && esbuild packages/cli/src/index.ts --bundle --platform=node --format=esm --loader:.md=text --banner:js=\"import { createRequire as __cjsRequire } from 'node:module'; const require = __cjsRequire(import.meta.url);\" --outfile=.test-dist/cli-under-test.mjs --log-level=warning && node --test \".test-dist/**/*.test.js\"",
		"bundle": "esbuild packages/cli/src/index.ts --bundle --platform=node --format=esm --loader:.md=text --banner:js=\"import { createRequire as __cjsRequire } from 'node:module'; const require = __cjsRequire(import.meta.url);\" --outfile=plugin/dist/cli.mjs"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"esbuild": "^0.24.0",
		"typescript": "^5.7.0"
	}
}
