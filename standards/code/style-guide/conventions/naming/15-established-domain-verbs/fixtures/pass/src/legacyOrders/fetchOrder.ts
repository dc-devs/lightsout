// This domain standardized on `fetch` before the vocabulary existed, and the
// carve-out lets it keep its verb rather than mix two.
export const fetchOrder = ({ id }: { id: string }): string => id;
