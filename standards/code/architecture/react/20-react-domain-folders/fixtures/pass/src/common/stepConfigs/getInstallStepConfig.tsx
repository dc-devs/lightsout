// Both builders graduated into a named domain folder beside `utils/`, so the
// group is visible from the file tree rather than inferred from the names.
export const getInstallStepConfig = () => ({ title: 'Install', body: <p>Run the installer.</p> });
